Global setup:
  git config --global user.name "Your Name"
  git config --global user.email mofeng@netease.com
      
Next steps:
  mkdir demo
  cd demo
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:mtslong/demo.git
  git push -u origin master
  
测试项目HelloWorld: 
  github路径：https://github.com/mtslong/HelloWorld.git
  本地路径：E:\03.git\github\HelloWorld
  生成克隆库操作：
  1、进入E:\03.git\github\目录；
  2、进入git Bash命令行；
  3、执行命令git clone https://github.com/mtslong/HelloWorld.git
  即可看到生成HelloWorld目录。剩下的操作和git操作一样。
  
测试发现github网页上的内容更新太慢了。
    